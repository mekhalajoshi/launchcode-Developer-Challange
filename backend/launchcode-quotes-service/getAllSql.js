exports.getAllSql = "Select list.id, c.name, list.destination, list.price	from (SELECT q.quote_id as id, q.price as price, l.location as destination, q.customer_contact_info_user_id  	FROM quotes q	INNER JOIN locations l	on l.location_id = q.destination_location	ORDER BY quote_date DESC 	LIMIT 9) list	INNER JOIN customer_contact_info c on c.user_id = list.customer_contact_info_user_id;";